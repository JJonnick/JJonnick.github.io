name: Pull request

on:
  pull_request: 
    branches: [ main ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: pnpm/action-setup@latest
      - name: Check changes
        run: pnpm run check