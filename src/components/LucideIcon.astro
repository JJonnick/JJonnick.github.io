---
import { icons, type LucideProps } from "@lucide/astro";

interface Props extends LucideProps {
    name: keyof typeof icons;
}

const { name, ...restProps } = Astro.props;
const Icon = icons[name];
---

<Icon {...restProps} />
